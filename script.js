// Check if the current browser supports standalone mode
if (window.navigator.standalone === true) {
  // The app is running in standalone mode, hide browser UI
  // and perform any other customizations as needed
  // Example: document.body.style.backgroundColor = '#ff0000';
}
